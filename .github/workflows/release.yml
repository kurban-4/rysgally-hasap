name: Release Build
on:
  workflow_dispatch: # Позволяет запустить сборку вручную кнопкой

jobs:
  build:
    runs-on: windows-latest # Собираем именно на Windows сервере!
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install PHP
        run: |
          # GitHub сам скачает PHP для Windows!
          choco install php --version 8.2.0

      - name: Install Dependencies
        run: |
          npm install
          cd src-tauri
          cargo fetch

      - name: Build Tauri App
        run: npm run tauri build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SirinNabat-Installer
          path: src-tauri/target/release/bundle/msi/*.msi